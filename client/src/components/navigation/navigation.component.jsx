// This is the navigation bar where you can select to sign in, sign out and register.

import React from 'react';

const Navigation = () => {
    return(
    <div>
        <nav>
            <p>Sign Out</p>
        </nav>
    </div>
    );
}

export default Navigation;